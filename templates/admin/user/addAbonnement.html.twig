{% extends 'base/base_admin.html.twig' %}

{% block title %}
Ajouter un abonnement
{% endblock %}

{% block body %}

<h1>
    Ajouter un abonnement à {{ user.prenom }} {{ user.nom }}
</h1>

{{ form_start(form) }}

    {{ form_row(form.abonnement) }}
    {{ form_row(form.moyenPaiement) }}

    <button class="btn btn-primary">
        Ajouter l'abonnement
    </button>

{{ form_end(form) }}

{% endblock %}