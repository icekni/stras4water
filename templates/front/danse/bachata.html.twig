{% extends 'base/base.html.twig' %}

{% block title %}Cours de Bachata - Stras4Water{% endblock %}

{% block body %}
<section class="container my-5">

<h1 class="mb-4 text-center text-success">Cours de Bachata à Strasbourg</h1>

  <!-- Description -->
  <div class="mb-5">
    <p>
      La <strong>bachata</strong> est une danse latine originaire de la République Dominicaine, aujourd’hui pratiquée dans le monde entier. 
      Chez Stras4Water, nos cours sont ouverts à tous, quel que soit votre niveau ou vos moyens, dans une ambiance chaleureuse, bienveillante et inclusive.
    </p>

    <div class="text-center my-4">
      <img src="{{ asset('img/bachata-cours.jpeg') }}" alt="Cours de bachata Stras4Water" class="img-fluid rounded shadow-sm" style="max-height: 400px;">
    </div>

    <p>
      Nous enseignons principalement la <strong>bachata moderne et la bachata sensuelle</strong>, deux styles très populaires en Europe. 
      Il nous arrive également d’y glisser quelques pas de bachata dominicaine, pour enrichir la musicalité et la diversité de vos mouvements.
    </p>

    <p>
        Nos cours sont idéaux si vous souhaitez <strong>ajouter un jour de pratique</strong> en complément d’une école de danse classique, mais aussi si vous n’avez pas les moyens d’en fréquenter une : notre mission est de rendre la danse <strong>accessible financièrement à tous</strong>.
    </p>

    <p>
      <strong>Les horaires pour la saison 2025/2026 (à partir de septembre 2025) sont en cours d’élaboration.</strong><br>
      Lors de la saison précédente, les cours avaient lieu chaque mercredi soir à 19h30.
    </p>

    <p>
      Les cours sont encadrés par une équipe dynamique : <strong>Jeremy, Coralie, Margo, Anna, Diane, Amine</strong> et <strong>Paul</strong>.
    </p>

    <h2 class="mt-5">Tarifs</h2> 
    <small>(adhésion à l'association obligatoire - 10€)</small>
    <ul>
      <li><strong>60€</strong> l’année</li>
      <li><strong>40€</strong> le semestre</li>
      <li><strong>30€</strong> la carte de 10 cours</li>
    </ul>
    <strong>Tarif réduit (50%)</strong> pour les personnes en situation précaire.
    <br><em>Un justificatif sera demandé lors de votre première séance.</em>

    <p>
      Grâce à ce système de tarification solidaire, <strong>nos cours sont financièrement très accessibles</strong>, car nous croyons que la danse devrait être ouverte à toutes et tous.
    </p>

    <div class="text-center my-4">
      <img src="{{ asset('img/bachata.jpeg') }}" alt="Groupe de danseurs Stras4Water" class="img-fluid rounded shadow-sm" style="max-height: 400px;">
    </div>

    <p class="mt-4">
      Pour toute question, n’hésitez pas à <a href="/contact">nous contacter</a> ou à venir nous rencontrer directement lors d’un cours ou événement Stras4Water.
    </p>
  </div>

  <!-- Type d'abonnement -->
  <h3 class="h5 mb-3">Choisissez votre type d'abonnement :</h3>

  <form method="post" action="{{ path('cart_add_item') }}" enctype="multipart/form-data">
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-3 mb-4">
      {% for abonnement in abonnements %}
        <div class="col">
          <input type="radio" class="btn-check abonnement-radio"
                name="item"
                id="abonnement-{{ abonnement.id }}"
                value="abonnement-{{ abonnement.id }}"
                data-has-reduit="{{ abonnement.hasTarifReduit ? '1' : '0' }}"
                required>
          <label class="btn btn-outline-primary w-100 text-start" for="abonnement-{{ abonnement.id }}">
            <strong>{{ abonnement.nom }}</strong><br>
            Du {{ abonnement.saison.dateDebut|date("d-m-Y") }} au {{ abonnement.saison.dateFin|date("d-m-Y") }}<br>
            Tarif normal : {{ abonnement.tarif }} €{% if abonnement.hasTarifReduit %}, réduit : {{ abonnement.tarifReduit }} €{% endif %}
          </label>
        </div>
      {% endfor %}

      {% for carte in cartes %}
        <div class="col">
          <input type="radio" class="btn-check carte-radio"
                name="item"
                id="carte-{{ carte.id }}"
                value="carte-{{ carte.id }}"
                data-has-reduit="{{ carte.hasTarifReduit ? '1' : '0' }}"
                required>
          <label class="btn btn-outline-primary w-100 text-start" for="carte-{{ carte.id }}">
            <strong>{{ carte.nom }}</strong><br>
            Tarif normal : {{ carte.tarif }} €{% if carte.hasTarifReduit %}, réduit : {{ carte.tarifReduit }} €{% endif %}
          </label>
        </div>
      {% endfor %}
    </div>

    <!-- Tarif réduit -->
    <div id="tarif-container" class="d-none">
      <h3 class="h5 mb-3">Choisissez votre tarif :</h3>
      <div class="d-flex gap-3 mb-4">
        <div>
          <input type="radio" class="btn-check" name="tarif" id="tarif_normal" value="normal" required>
          <label class="btn btn-outline-secondary" for="tarif_normal">Tarif normal</label>
        </div>
        <div>
          <input type="radio" class="btn-check" name="tarif" id="tarif_reduit" value="reduit">
          <label class="btn btn-outline-secondary" for="tarif_reduit">Tarif réduit</label>
        </div>
      </div>
    </div>

    <!-- Justificatif info -->
    <div class="alert alert-warning d-none" id="justificatif-wrapper">
      ⚠️ Vous avez choisi le tarif réduit : <strong>un justificatif vous sera demandé lors de votre première séance</strong>. Merci de le prévoir.
    </div>

    <!-- Alerte adhésion -->
    <div class="alert alert-info mt-4">
      ⚠️ <strong>L'adhésion annuelle est obligatoire</strong> pour toute souscription.
      Elle est automatiquement ajoutée au panier si vous n'êtes pas encore adhérent(e).
      <br><br>
      <strong>Si vous n'êtes pas connecté(e)</strong> : vous serez invité à vous connecter ou créer un compte.
    </div>

    <button type="submit" class="btn btn-primary mt-3">S'inscrire</button>
  </form>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const radiosAbonnement = document.querySelectorAll('.abonnement-radio, .carte-radio');
    const tarifContainer = document.getElementById('tarif-container');
    const justificatifWrapper = document.getElementById('justificatif-wrapper');
    const radiosTarif = document.querySelectorAll('input[name="tarif"]');

    radiosAbonnement.forEach(radio => {
      radio.addEventListener('change', () => {
        const hasReduit = radio.dataset.hasReduit === '1';

        if (hasReduit) {
          tarifContainer.classList.remove('d-none');
        } else {
          tarifContainer.classList.add('d-none');
          justificatifWrapper.classList.add('d-none');
          radiosTarif.forEach(t => t.checked = false);
        }
      });
    });

    radiosTarif.forEach(radio => {
      radio.addEventListener('change', () => {
        if (radio.value === 'reduit') {
          justificatifWrapper.classList.remove('d-none');
        } else {
          justificatifWrapper.classList.add('d-none');
        }
      });
    });
  });
</script>
{% endblock %}
